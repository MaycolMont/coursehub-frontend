# URL base de la API (backend en Render en producción).
# En Vercel define esta misma variable en Settings -> Environment Variables.
# Si se deja vacía, las peticiones van al mismo origen (proxy de Vite en desarrollo).
VITE_API_URL=